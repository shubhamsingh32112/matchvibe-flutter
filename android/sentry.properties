# Sentry CLI / Gradle plugin defaults (Match Vibe Flutter app).
# Auth token must NOT be committed — set SENTRY_AUTH_TOKEN in CI or locally.
defaults.url=https://sentry.io/
defaults.org=yagati
defaults.project=flutter
